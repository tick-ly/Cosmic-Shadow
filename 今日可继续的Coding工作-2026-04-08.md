# 今日可继续的 Coding 工作

日期：`2026-04-08`

## 说明

本清单用于记录当前项目里仍然适合继续推进的 coding 工作，按模块归类，便于后续继续拆分和执行。

## 核心玩法

1. 做 `节点占领系统`
   - 为 `MapNode` 增加占领状态、占领进度、阵营切换规则
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Core/MapNode.cs`

2. 做 `探索/遭遇结算器`
   - 新增纯 C# 的 `NodeEncounterResolver`
   - 把“到达节点后发生什么”从控制器里拆出去
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Strategy/PlayerCommander.cs`

3. 做 `回合推进闭环`
   - 串联 `GameState`、`MovementManager`、`BattleManager`
   - 形成完整回合流
   - 文件参考：
     - `UnityProject/Assets/_Project/Scripts/Core/GameState.cs`
     - `UnityProject/Assets/_Project/Scripts/Systems/MovementManager.cs`
     - `UnityProject/Assets/_Project/Scripts/Systems/BattleManager.cs`

4. 做 `敌对节点规则`
   - 定义哪些节点触发战斗、哪些节点安全、哪些节点提供资源或事件

## 地图与移动

1. 做 `航线渲染系统`
   - 在 `MapManager` 中生成并管理 `LineRenderer`
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Systems/MapManager.cs`

2. 做 `路径高亮`
   - 选中节点或单位时，高亮可达节点和对应航线

3. 做 `路径跟随移动`
   - 升级 `CombatUnitController` 的移动逻辑
   - 不再只是直线 `MoveTowards`
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Strategy/CombatUnitController.cs`

4. 做 `镜头控制`
   - 缩放、拖拽、聚焦当前单位或目标节点

5. 做 `战争迷雾刷新优化`
   - 稳定可见性刷新和探索反馈
   - 文件参考：
     - `UnityProject/Assets/_Project/Scripts/Systems/MapManager.cs`
     - `UnityProject/Assets/_Project/Scripts/Core/TerrainCombatResolver.cs`

## UI

1. 做 `RiskAssessmentPanel`
   - 显示目标节点、当前现实债务、技能列表
   - 选中技能后实时计算修正成功率
   - 点击后调用 `CombatResolver`

2. 做 `主 HUD`
   - 把 `GameUIManager` 中的空引用接到真实 UI 控件
   - 文件参考：`UnityProject/Assets/_Project/Scripts/UI/GameUIManager.cs`

3. 做 `现实债务警报 UI`
   - 监听 `DEBT_WARNING` 和 `DEBT_CRITICAL`
   - 做警告槽和危险阈值表现
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Core/EventManager.cs`

4. 做 `节点详情面板`
   - 显示地形、控制方、危险等级、可移动目标

5. 做 `战斗日志面板`
   - 把事件流真正转成可用 UI，而不是只靠调试输出

## 数据驱动

1. 做 `数据加载入口`
   - 新增 `GameDataBootstrap` 或 `SkillDatabase`
   - 统一加载 `SkillDataSO` 和 `CharacterClassSO`
   - 文件参考：
     - `UnityProject/Assets/_Project/Scripts/Data/SkillDataSO.cs`
     - `UnityProject/Assets/_Project/Scripts/Data/CharacterClassSO.cs`

2. 做 `默认数据回退逻辑`
   - 避免缺资产时到处使用临时代码

3. 做 `地图生成配置接入`
   - 把 `MapGenerationConfigSO` 真正接入地图生成流程
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Data/MapGenerationConfigSO.cs`

4. 做 `单位数据接入`
   - 把 `CombatUnitDataSO` 接入单位生成和表现层
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Data/CombatUnitDataSO.cs`

## 编辑器与工具

1. 做 `一键场景初始化工具`
   - 补强 `SceneInitializer`
   - 自动创建并挂接必要引用
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Systems/SceneInitializer.cs`

2. 做 `SO 批量创建工具`
   - 快速生成技能、职业、地图配置资产

3. 做 `地图调试面板`
   - 显示节点连接、可见性、移动范围、控制权

4. 做 `Terrain 导入编辑器工具`
   - 自动把地形生成产物接入 Unity Terrain
   - 文件参考：`tools/generate_battlefield_terrain.py`

## 质量与测试

1. 给 `SkillBase` 补单元测试
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Core/SkillBase.cs`

2. 给 `CombatResolver` 补单元测试
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Core/CombatResolver.cs`

3. 给 `MovementManager` 补可移动规则测试
   - 文件参考：`UnityProject/Assets/_Project/Scripts/Systems/MovementManager.cs`

4. 做 `最小冒烟测试场景`
   - 保证每次改完还能跑通一轮核心流程

## 当前最值得先做的 5 项

1. `RiskAssessmentPanel`
2. `节点到战斗触发链`
3. `航线渲染 + 路径高亮`
4. `数据加载入口`
5. `债务警报 UI`

## 推荐顺序

建议优先执行：

1. `RiskAssessmentPanel`
2. `节点到战斗触发链`
3. `主 HUD + 债务警报 UI`
4. `航线渲染 + 路径高亮`
5. `数据加载入口`
